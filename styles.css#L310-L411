/* 按钮网格布局优化 */
.keypad {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(80px, 1fr)); /* 自适应列数 */
    gap: 10px;
}

button {
    height: 60px;
    min-height: 45px; /* 设置最小高度 */
    font-size: 18px;
    border-radius: 5px;
    transition: transform 0.1s;
    position: relative;
    background-color: #FBD000;
    color: #000;
    box-shadow: 0 5px 0 #B29700;
}

/* 新增小屏幕样式 */
@media (max-width: 480px) {
    button {
        font-size: 14px;
    }
}